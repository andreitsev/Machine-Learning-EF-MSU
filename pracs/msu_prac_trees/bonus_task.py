import numpy as np
import pandas as pd
import os


def visualize_tree(
        tree: dict,
        verbose: bool=True,
        save_path: str=None
) -> None:

    """
    Функция, которая принимает на вход обученное дерево в виде словаря (DecisionTree._tree из файла submission.py)
    и записывает в текстовый файл (save_path) визуалиазацию данного дерева

    Args:
        tree: объект DecisionTree._tree
        verbose: если True, то вывести на экран визуализацию
        save_path: если не None, то записать визуализацию в файл
    Returns:
        None
    """

    pass

    return




